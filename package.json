{
  "name": "pipr-sdr-coach",
  "version": "0.1.0",
  "private": true,
  "description": "SDR Accountability Coach — AI agent that holds salespeople accountable through voice calls",
  "scripts": {
    "dev": "npx convex dev",
    "deploy": "npx convex deploy",
    "push": "npx convex deploy --cmd 'echo deployed'"
  },
  "dependencies": {
    "convex": "^1.17.0"
  }
}
